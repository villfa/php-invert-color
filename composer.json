{
    "name": "villfa/invert-color",
    "description": "InvertColor - invert hex color code",
    "keywords": [
        "color",
        "colour",
        "invert",
        "inverse",
        "reverse",
        "opposite"
    ],
    "license": ["MIT"],
    "type": "library",
    "authors": [
        {
            "name": "Fabien Villepinte",
            "email": "fabien.villepinte@gmail.com"
        }
    ],
    "autoload": {
        "psr-4": { "InvertColor\\": "src/" }
    },
    "autoload-dev": {
        "psr-4": { "InvertColor\\Tests\\": "tests/" }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "require": {
        "php": "^7.1"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^2.14",
        "jakub-onderka/php-parallel-lint": "^1.0",
        "phpstan/phpstan": "^0.10",
        "phpstan/phpstan-phpunit": "^0.10",
        "phpstan/phpstan-strict-rules": "^0.10",
        "phpunit/phpunit": "^7.0"
    },
    "scripts": {
        "test": [
            "@composer validate --no-interaction --strict",
            "parallel-lint src/ tests/",
            "phpunit",
            "phpstan analyze src tests --level=max",
            "php-cs-fixer fix --rules=@PSR2 -v --dry-run --stop-on-violation --using-cache=no ."
        ],
        "ut": [
            "phpunit"
        ]
    },
    "scripts-descriptions": {
        "test": "Validate and test the whole library.",
        "ut": "Run unit tests only."
    }
}
